[project]
name = "stock_bot"
version = "0.1.0"
description = "Swing trading assistant — scaffold (Phase 1)"
authors = ["Automated Scaffold <no-reply@example.com>"]

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = ["tests"]
